\section{Core Skills}

\begin{itemize}[leftmargin=0.15in, label={}]
    \item{
    \textbf{Designing, building and operating an MLOps platform on on-prem Kubernetes:}
    \\
    I codified the entire path — ingestion $\rightarrow$ lakehouse $\rightarrow$ transformation $\rightarrow$ lineage $\rightarrow$ distributed training $\rightarrow$ experiment / model registry — on bare-metal Kubernetes in \textbf{Pulumi (Python IaC)}, and I build and operate it alone. Across networking (Cilium), storage (SeaweedFS) and identity (Dex OIDC $\rightarrow$ Kubernetes RBAC) I \textbf{authored the majority of all 12 stacks} (93\% of stack-code changes) and run \textbf{10 nodes / 235 running pods / 23 internal services} in production.
    }
    \item{
    \textbf{Data pipelines and the lakehouse:}
    \\
    Designed and implemented a medallion pipeline on \textbf{Airflow 3.x + dbt (Cosmos) + Apache Iceberg} with lineage auto-recorded in \textbf{DataHub}; authored 83\% of the ETL repository and 93\% of its shared framework. Previously ran large-scale cloud pipelines on AWS EMR / PySpark.
    }
    \item{
    \textbf{Architecture and trade-off decisions made by measurement:}
    \\
    I measure before choosing tools. Turned a GPU purchase request into reclamation with 10 days of telemetry (\textbf{5.3\% utilization, zero queued jobs, 1,112 GPU-hours held idle}), cut training-data delivery \textbf{25$\times$} by redesigning network and storage, and designed a reproducibility contract from a quantified survey of the research workflow. Weighing cost, performance and operational complexity is the same habit that drove earlier decisions such as adopting PGVector.
    }
    \item{
    \textbf{AI agents run continuously, backed by verification gates:}
    \\
    I run AI coding agents continuously to lift \textbf{one person's throughput to team scale} (678 merged PRs in 6 months), and keep that speed honest with \textbf{719 SDK unit tests}, CI gates and pre-commit hooks that block tag/version drift and private-key commits, and \textbf{23 incident write-ups and 30 PRDs}. Earlier, built deduplication systems and chatbot prototypes with LangChain and embedding models.
    }
    \item{
    \textbf{Full-stack breadth and team leadership:}
    \\
    A path from iOS through backend (FastAPI / Flask / Spring Boot), frontend (Next.js) and cloud (AWS / GCP) let me lead a data engineering team; today I design and enforce the split where \textbf{researchers own domain ETL and the platform owns execution, lineage and verification}.
    }
\end{itemize}
